// Package ebook was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package ebook

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/ebookcontacts"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

// The cursor class structure.
type BookClientCursorClassGType struct {
	_ structs.HostLayout

	ParentClass gobject.ObjectClassGType

	xRefresh uintptr
}

func (x *BookClientCursorClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

// OverrideRefresh sets the "refresh" callback function.
// The class handler for the #EBookClientCursor::refresh signal
func (x *BookClientCursorClassGType) OverrideRefresh(cb func(*BookClientCursor)) {
	if cb == nil {
		x.xRefresh = 0
	} else {
		x.xRefresh = purego.NewCallback(func(CursorVarp uintptr) {
			cb(BookClientCursorNewFromInternalPtr(CursorVarp))
		})
	}
}

// GetRefresh gets the "refresh" callback function.
// The class handler for the #EBookClientCursor::refresh signal
func (x *BookClientCursorClassGType) GetRefresh() func(*BookClientCursor) {
	if x.xRefresh == 0 {
		return nil
	}
	var rawCallback func(CursorVarp uintptr)
	purego.RegisterFunc(&rawCallback, x.xRefresh)
	return func(CursorVar *BookClientCursor) {
		rawCallback(CursorVar.GoPointer())
	}
}

type BookClientCursorPrivate struct {
	_ structs.HostLayout
}

func (x *BookClientCursorPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

// Contains only private data.
type BookClientCursor struct {
	gobject.Object
}

var xBookClientCursorGLibType func() types.GType

func BookClientCursorGLibType() types.GType {
	return xBookClientCursorGLibType()
}

func BookClientCursorNewFromInternalPtr(ptr uintptr) *BookClientCursor {
	cls := &BookClientCursor{}
	cls.Ptr = ptr
	return cls
}

var xBookClientCursorGetAlphabet func(uintptr, *int, *int, *int, *int) []string

// Fetches the array of displayable labels for the &lt;link linkend="cursor-alphabet"&gt;active alphabet&lt;/link&gt;.
//
// The active alphabet is based on the current locale configuration of the
// addressbook, and can be a different alphabet for locales requiring non-Latin
// language scripts. These UTF-8 labels are appropriate to display in a user
// interface to represent the alphabetic position of the cursor in the user's
// native alphabet.
//
// The @underflow, @inflow and @overflow parameters allow one to observe which
// indexes Evolution Data Server is using to store words which sort outside
// of the alphabet, for instance words from foreign language scripts and
// words which start with numeric characters, or other types of character.
//
// While the @underflow and @overflow are for words which sort below or
// above the active alphabets, the @inflow index is for words which sort
// in between multiple concurrently active alphabets. The active alphabet
// array might contain more than one alphabet for locales where it is
// very common or expected to have names in Latin script as well as names
// in another script.
func (x *BookClientCursor) GetAlphabet(NLabelsVar *int, UnderflowVar *int, InflowVar *int, OverflowVar *int) []string {

	cret := xBookClientCursorGetAlphabet(x.GoPointer(), NLabelsVar, UnderflowVar, InflowVar, OverflowVar)
	return cret
}

var xBookClientCursorGetContactAlphabeticIndex func(uintptr, uintptr) int

// Checks which alphabetic index @contact would be sorted
// into according to @cursor.
//
// So long as the active #EBookClientCursor:alphabet does
// not change, the returned index will be a valid position
// in the array of labels returned by e_book_client_cursor_get_alphabet().
//
// If the index returned by this function is needed for
// any extended period of time, it should be recalculated
// whenever the #EBookClientCursor:alphabet changes.
func (x *BookClientCursor) GetContactAlphabeticIndex(ContactVar *ebookcontacts.Contact) int {

	cret := xBookClientCursorGetContactAlphabeticIndex(x.GoPointer(), ContactVar.GoPointer())
	return cret
}

var xBookClientCursorGetPosition func(uintptr) int

// Fetches the number of contacts leading up to the current
// cursor position, inclusive of the current cursor position.
//
// The position value can be anywhere from 0 to the total
// number of contacts plus one. A value of 0 indicates
// that the cursor is positioned before the contact list in
// the symbolic %E_BOOK_CURSOR_ORIGIN_BEGIN state. If
// the position is greater than the total, as returned by
// e_book_client_cursor_get_total(), then the cursor is positioned
// after the last contact in the symbolic %E_BOOK_CURSOR_ORIGIN_END position.
func (x *BookClientCursor) GetPosition() int {

	cret := xBookClientCursorGetPosition(x.GoPointer())
	return cret
}

var xBookClientCursorGetTotal func(uintptr) int

// Fetches the total number of contacts in the addressbook
// which match @cursor's query
func (x *BookClientCursor) GetTotal() int {

	cret := xBookClientCursorGetTotal(x.GoPointer())
	return cret
}

var xBookClientCursorRefClient func(uintptr) uintptr

// Returns the #EBookClientCursor:client associated with @cursor.
//
// The returned #EBookClient is referenced because the cursor
// does not keep a strong reference to the client.
//
// Unreference the #EBookClient with g_object_unref() when finished with it.
func (x *BookClientCursor) RefClient() *BookClient {
	var cls *BookClient

	cret := xBookClientCursorRefClient(x.GoPointer())

	if cret == 0 {
		return nil
	}
	cls = &BookClient{}
	cls.Ptr = cret
	return cls
}

var xBookClientCursorSetAlphabeticIndex func(uintptr, int, uintptr, uintptr, uintptr)

// Sets the current cursor position to point to an &lt;link linkend="cursor-alphabet"&gt;Alphabetic Index&lt;/link&gt;.
//
// See: e_book_client_cursor_set_alphabetic_index_sync().
//
// This asynchronous call is completed with a call to
// e_book_client_cursor_set_alphabetic_index_finish() from the specified @callback.
func (x *BookClientCursor) SetAlphabeticIndex(IndexVar int, CancellableVar *gio.Cancellable, CallbackVar *gio.AsyncReadyCallback, UserDataVar uintptr) {

	xBookClientCursorSetAlphabeticIndex(x.GoPointer(), IndexVar, CancellableVar.GoPointer(), glib.NewCallbackNullable(CallbackVar), UserDataVar)

}

var xBookClientCursorSetAlphabeticIndexFinish func(uintptr, uintptr, **glib.Error) bool

// Completes an asynchronous call initiated by e_book_client_cursor_set_alphabetic_index().
func (x *BookClientCursor) SetAlphabeticIndexFinish(ResultVar gio.AsyncResult) (bool, error) {
	var cerr *glib.Error

	cret := xBookClientCursorSetAlphabeticIndexFinish(x.GoPointer(), ResultVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xBookClientCursorSetAlphabeticIndexSync func(uintptr, int, uintptr, **glib.Error) bool

// Sets the cursor to point to an &lt;link linkend="cursor-alphabet"&gt;Alphabetic Index&lt;/link&gt;.
//
// After setting the alphabetic index, for example the
// index for letter 'E', then further calls to e_book_client_cursor_step()
// will return results starting with the letter 'E' (or results starting
// with the last result in 'D' when navigating through cursor results
// in reverse).
//
// The passed index must be a valid index into the alphabet parameters
// returned by e_book_client_cursor_get_alphabet().
//
// If this method is called from the same thread context in which
// the cursor was created, then the updates to the #EBookClientCursor:position
// property are guaranteed to be delivered synchronously upon successful completion
// of moving the cursor. Otherwise, notifications will be delivered asynchronously
// in the cursor's original thread context.
//
// If this method completes with an %E_CLIENT_ERROR_OUT_OF_SYNC error, it is an
// indication that the addressbook has been set into a new locale and it would be
// unsafe to set the alphabetic index at this time. If you receive an out of sync
// error from this method, then you should wait until an #EBookClientCursor:alphabet
// property change notification is delivered and then proceed to load the new
// alphabet before trying to set any alphabetic index.
func (x *BookClientCursor) SetAlphabeticIndexSync(IndexVar int, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := xBookClientCursorSetAlphabeticIndexSync(x.GoPointer(), IndexVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xBookClientCursorSetSexp func(uintptr, string, uintptr, uintptr, uintptr)

// Sets the &lt;link linkend="cursor-search"&gt;Search Expression&lt;/link&gt; for the cursor.
//
// See: e_book_client_cursor_set_sexp_sync().
//
// This asynchronous call is completed with a call to
// e_book_client_cursor_set_sexp_finish() from the specified @callback.
func (x *BookClientCursor) SetSexp(SexpVar string, CancellableVar *gio.Cancellable, CallbackVar *gio.AsyncReadyCallback, UserDataVar uintptr) {

	xBookClientCursorSetSexp(x.GoPointer(), SexpVar, CancellableVar.GoPointer(), glib.NewCallbackNullable(CallbackVar), UserDataVar)

}

var xBookClientCursorSetSexpFinish func(uintptr, uintptr, **glib.Error) bool

// Completes an asynchronous call initiated by e_book_client_cursor_set_sexp(), reporting
// whether the new search expression was accepted.
func (x *BookClientCursor) SetSexpFinish(ResultVar gio.AsyncResult) (bool, error) {
	var cerr *glib.Error

	cret := xBookClientCursorSetSexpFinish(x.GoPointer(), ResultVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xBookClientCursorSetSexpSync func(uintptr, string, uintptr, **glib.Error) bool

// Sets the &lt;link linkend="cursor-search"&gt;Search Expression&lt;/link&gt; for the cursor.
//
// A side effect of setting the search expression is that the
// &lt;link linkend="cursor-pos-total"&gt;position and total&lt;/link&gt;
// properties will be updated.
//
// If this method is called from the same thread context in which
// the cursor was created, then the updates to the #EBookClientCursor:position
// and #EBookClientCursor:total properties are guaranteed to be delivered
// synchronously upon successful completion of setting the search expression.
// Otherwise, notifications will be delivered asynchronously in the cursor's
// original thread context.
//
// If the backend does not support the given search expression,
// an %E_CLIENT_ERROR_INVALID_QUERY error will be set.
func (x *BookClientCursor) SetSexpSync(SexpVar string, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := xBookClientCursorSetSexpSync(x.GoPointer(), SexpVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xBookClientCursorStep func(uintptr, ebookcontacts.BookCursorStepFlags, ebookcontacts.BookCursorOrigin, int, uintptr, uintptr, uintptr)

// &lt;link linkend="cursor-iteration"&gt;Steps the cursor through the results&lt;/link&gt; by
// a maximum of @count and fetch the results traversed.
//
// See: e_book_client_cursor_step_sync().
//
// This asynchronous call is completed with a call to
// e_book_client_cursor_step_finish() from the specified @callback.
func (x *BookClientCursor) Step(FlagsVar ebookcontacts.BookCursorStepFlags, OriginVar ebookcontacts.BookCursorOrigin, CountVar int, CancellableVar *gio.Cancellable, CallbackVar *gio.AsyncReadyCallback, UserDataVar uintptr) {

	xBookClientCursorStep(x.GoPointer(), FlagsVar, OriginVar, CountVar, CancellableVar.GoPointer(), glib.NewCallbackNullable(CallbackVar), UserDataVar)

}

var xBookClientCursorStepFinish func(uintptr, uintptr, **glib.SList, **glib.Error) int

// Completes an asynchronous call initiated by e_book_client_cursor_step(), fetching
// any contacts which might have been returned by the call.
func (x *BookClientCursor) StepFinish(ResultVar gio.AsyncResult, OutContactsVar **glib.SList) (int, error) {
	var cerr *glib.Error

	cret := xBookClientCursorStepFinish(x.GoPointer(), ResultVar.GoPointer(), OutContactsVar, &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xBookClientCursorStepSync func(uintptr, ebookcontacts.BookCursorStepFlags, ebookcontacts.BookCursorOrigin, int, **glib.SList, uintptr, **glib.Error) int

// &lt;link linkend="cursor-iteration"&gt;Steps the cursor through the results&lt;/link&gt; by
// a maximum of @count and fetch the results traversed.
//
// If @count is negative, then the cursor will move backwards.
//
// If @cursor reaches the beginning or end of the query results, then the
// returned list might not contain the amount of desired contacts, or might
// return no results if the cursor currently points to the last contact.
// Reaching the end of the list is not considered an error condition. Attempts
// to step beyond the end of the list after having reached the end of the list
// will however trigger an %E_CLIENT_ERROR_QUERY_REFUSED error.
//
// If %E_BOOK_CURSOR_STEP_FETCH is specified in @flags, a pointer to
// a %NULL #GSList pointer should be provided for the @results parameter.
//
// If %E_BOOK_CURSOR_STEP_MOVE is specified in @flags, then the cursor's
// state will be modified and the &lt;link linkend="cursor-pos-total"&gt;position&lt;/link&gt;
// property will be updated as a result.
//
// If this method is called from the same thread context in which
// the cursor was created, then the updates to the #EBookClientCursor:position
// property are guaranteed to be delivered synchronously upon successful completion
// of moving the cursor. Otherwise, notifications will be delivered asynchronously
// in the cursor's original thread context.
//
// If this method completes with an %E_CLIENT_ERROR_OUT_OF_SYNC error, it is an
// indication that the addressbook has been modified and it would be unsafe to
// move the cursor at this time. Any %E_CLIENT_ERROR_OUT_OF_SYNC error is guaranteed
// to be followed by an #EBookClientCursor::refresh signal at which point any content
// should be reloaded.
func (x *BookClientCursor) StepSync(FlagsVar ebookcontacts.BookCursorStepFlags, OriginVar ebookcontacts.BookCursorOrigin, CountVar int, OutContactsVar **glib.SList, CancellableVar *gio.Cancellable) (int, error) {
	var cerr *glib.Error

	cret := xBookClientCursorStepSync(x.GoPointer(), FlagsVar, OriginVar, CountVar, OutContactsVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func (c *BookClientCursor) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *BookClientCursor) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// GetPropertyAlphabet gets the "alphabet" property.
// The currently &lt;link linkend="cursor-alphabet"&gt;active alphabet&lt;/link&gt;.
//
// The value is a %NULL terminated array of strings,
// each string is suitable to display a specific letter
// in the active alphabet.
//
// Indexes from this array can later be used with
// e_book_client_cursor_set_alphabetic_index().
//
// This property will automatically change if the
// active locale of the addressbook server changes.
//
// Property change notifications are guaranteed to be
// delivered in the #GMainContext which was the thread
// default context at cursor creation time.
func (x *BookClientCursor) GetPropertyAlphabet() []string {
	var v gobject.Value
	x.GetProperty("alphabet", &v)
	return core.GoStringSlice(v.GetBoxed())
}

// SetPropertyContext sets the "context" property.
// The #GMainContext in which the #EBookClient created this cursor.
//
// &lt;note&gt;&lt;para&gt;This is an internal parameter for constructing the
// cursor, to construct the cursor use e_book_client_get_cursor().
// &lt;/para&gt;&lt;/note&gt;
func (x *BookClientCursor) SetPropertyContext(value uintptr) {
	var v gobject.Value
	v.Init(gobject.TypePointerVal)
	v.SetPointer(value)
	x.SetProperty("context", &v)
}

// SetPropertyDirectCursor sets the "direct-cursor" property.
// The direct handle to the #EDataBookCursor for direct read access mode.
//
// &lt;note&gt;&lt;para&gt;This is an internal parameter for constructing the
// cursor, to construct the cursor use e_book_client_get_cursor().
// &lt;/para&gt;&lt;/note&gt;
func (x *BookClientCursor) SetPropertyDirectCursor(value uintptr) {
	var v gobject.Value
	v.Init(gobject.TypePointerVal)
	v.SetPointer(value)
	x.SetProperty("direct-cursor", &v)
}

// SetPropertyObjectPath sets the "object-path" property.
// The D-Bus object path to find the server side cursor object.
//
// &lt;note&gt;&lt;para&gt;This is an internal parameter for constructing the
// cursor, to construct the cursor use e_book_client_get_cursor().
// &lt;/para&gt;&lt;/note&gt;
func (x *BookClientCursor) SetPropertyObjectPath(value string) {
	var v gobject.Value
	v.Init(gobject.TypeStringVal)
	v.SetString(value)
	x.SetProperty("object-path", &v)
}

// GetPropertyPosition gets the "position" property.
// The current cursor position in the cursor's result list.
//
// More specifically, the cursor position is defined as
// the number of contacts leading up to the current
// cursor position, inclusive of the current cursor
// position.
//
// If the position value is 0, then the cursor is positioned
// before the contact list in the symbolic %E_BOOK_CURSOR_ORIGIN_BEGIN
// position. If the position value is greater than
// #EBookClientCursor:total, this indicates that the cursor is
// positioned after the contact list in the symbolic
// %E_BOOK_CURSOR_ORIGIN_END position.
//
// Property change notifications are guaranteed to be
// delivered in the #GMainContext which was the thread
// default context at cursor creation time.
func (x *BookClientCursor) GetPropertyPosition() int {
	var v gobject.Value
	x.GetProperty("position", &v)
	return v.GetInt()
}

// SetPropertySortFields sets the "sort-fields" property.
// The #EContactField names to sort this cursor with
//
// &lt;note&gt;&lt;para&gt;This is an internal parameter for constructing the
// cursor, to construct the cursor use e_book_client_get_cursor().
// &lt;/para&gt;&lt;/note&gt;
func (x *BookClientCursor) SetPropertySortFields(value []string) {
	var v gobject.Value
	v.Init(glib.StrvGetType())
	v.SetBoxed(uintptr(unsafe.Pointer(core.ByteSlice(value))))
	x.SetProperty("sort-fields", &v)
}

// GetPropertyTotal gets the "total" property.
// The total number of contacts which satisfy the cursor's query.
//
// Property change notifications are guaranteed to be
// delivered in the #GMainContext which was the thread
// default context at cursor creation time.
func (x *BookClientCursor) GetPropertyTotal() int {
	var v gobject.Value
	x.GetProperty("total", &v)
	return v.GetInt()
}

// Indicates that the addressbook has been modified and
// that any content currently being displayed from the current
// cursor position should be reloaded.
//
// This signal is guaranteed to be delivered in the #GMainContext
// which was the thread default context at cursor creation time.
func (x *BookClientCursor) ConnectRefresh(cb *func(BookClientCursor)) uint32 {
	cbPtr := uintptr(unsafe.Pointer(cb))
	if cbRefPtr, ok := glib.GetCallback(cbPtr); ok {
		return gobject.SignalConnect(x.GoPointer(), "refresh", cbRefPtr)
	}

	fcb := func(clsPtr uintptr) {
		fa := BookClientCursor{}
		fa.Ptr = clsPtr
		cbFn := *cb

		cbFn(fa)

	}
	cbRefPtr := purego.NewCallback(fcb)
	glib.SaveCallback(cbPtr, cbRefPtr)
	return gobject.SignalConnect(x.GoPointer(), "refresh", cbRefPtr)
}

// Initializes the object implementing the interface.
//
// This method is intended for language bindings. If writing in C,
// g_initable_new() should typically be used instead.
//
// The object must be initialized before any real use after initial
// construction, either with this function or g_async_initable_init_async().
//
// Implementations may also support cancellation. If @cancellable is not %NULL,
// then initialization can be cancelled by triggering the cancellable object
// from another thread. If the operation was cancelled, the error
// %G_IO_ERROR_CANCELLED will be returned. If @cancellable is not %NULL and
// the object doesn't support cancellable initialization the error
// %G_IO_ERROR_NOT_SUPPORTED will be returned.
//
// If the object is not initialized, or initialization returns with an
// error, then all operations on the object except g_object_ref() and
// g_object_unref() are considered to be invalid, and have undefined
// behaviour. See the [description][iface@Gio.Initable#description] for more details.
//
// Callers should not assume that a class which implements #GInitable can be
// initialized multiple times, unless the class explicitly documents itself as
// supporting this. Generally, a class’ implementation of init() can assume
// (and assert) that it will only be called once. Previously, this documentation
// recommended all #GInitable implementations should be idempotent; that
// recommendation was relaxed in GLib 2.54.
//
// If a class explicitly supports being initialized multiple times, it is
// recommended that the method is idempotent: multiple calls with the same
// arguments should return the same results. Only the first call initializes
// the object; further calls return the result of the first call.
//
// One reason why a class might need to support idempotent initialization is if
// it is designed to be used via the singleton pattern, with a
// #GObjectClass.constructor that sometimes returns an existing instance.
// In this pattern, a caller would expect to be able to call g_initable_init()
// on the result of g_object_new(), regardless of whether it is in fact a new
// instance.
func (x *BookClientCursor) Init(CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGInitableInit(x.GoPointer(), CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("EBOOK", "libebook-1.2")
	core.SetSharedLibraries("EBOOK", []string{"libcamel-1.2.so.66", "libebook-1.2.so.21", "libebook-contacts-1.2.so.4", "libedata-book-1.2.so.27", "libedataserver-1.2.so.27"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("EBOOK") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xBookClientCursorGLibType, libs, "e_book_client_cursor_get_type")

	core.PuregoSafeRegister(&xBookClientCursorGetAlphabet, libs, "e_book_client_cursor_get_alphabet")
	core.PuregoSafeRegister(&xBookClientCursorGetContactAlphabeticIndex, libs, "e_book_client_cursor_get_contact_alphabetic_index")
	core.PuregoSafeRegister(&xBookClientCursorGetPosition, libs, "e_book_client_cursor_get_position")
	core.PuregoSafeRegister(&xBookClientCursorGetTotal, libs, "e_book_client_cursor_get_total")
	core.PuregoSafeRegister(&xBookClientCursorRefClient, libs, "e_book_client_cursor_ref_client")
	core.PuregoSafeRegister(&xBookClientCursorSetAlphabeticIndex, libs, "e_book_client_cursor_set_alphabetic_index")
	core.PuregoSafeRegister(&xBookClientCursorSetAlphabeticIndexFinish, libs, "e_book_client_cursor_set_alphabetic_index_finish")
	core.PuregoSafeRegister(&xBookClientCursorSetAlphabeticIndexSync, libs, "e_book_client_cursor_set_alphabetic_index_sync")
	core.PuregoSafeRegister(&xBookClientCursorSetSexp, libs, "e_book_client_cursor_set_sexp")
	core.PuregoSafeRegister(&xBookClientCursorSetSexpFinish, libs, "e_book_client_cursor_set_sexp_finish")
	core.PuregoSafeRegister(&xBookClientCursorSetSexpSync, libs, "e_book_client_cursor_set_sexp_sync")
	core.PuregoSafeRegister(&xBookClientCursorStep, libs, "e_book_client_cursor_step")
	core.PuregoSafeRegister(&xBookClientCursorStepFinish, libs, "e_book_client_cursor_step_finish")
	core.PuregoSafeRegister(&xBookClientCursorStepSync, libs, "e_book_client_cursor_step_sync")

}

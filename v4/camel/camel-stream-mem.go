// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type StreamMemClassGType struct {
	_ structs.HostLayout

	ParentClass StreamClassGType

	Reserved [20]uintptr
}

func (x *StreamMemClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamMemPrivate struct {
	_ structs.HostLayout
}

func (x *StreamMemPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamMem struct {
	Stream
}

var xStreamMemGLibType func() types.GType

func StreamMemGLibType() types.GType {
	return xStreamMemGLibType()
}

func StreamMemNewFromInternalPtr(ptr uintptr) *StreamMem {
	cls := &StreamMem{}
	cls.Ptr = ptr
	return cls
}

var xNewStreamMem func() uintptr

// Create a new #CamelStreamMem object.
func NewStreamMem() *StreamMem {
	var cls *StreamMem

	cret := xNewStreamMem()

	if cret == 0 {
		return nil
	}
	cls = &StreamMem{}
	cls.Ptr = cret
	return cls
}

var xNewStreamMemWithBuffer func([]byte, uint) uintptr

// Create a new memory stream using @buffer as the stream data.
//
// Note: @buffer will be copied into an internal #GByteArray structure
// for use as the stream backing. This may have resource implications
// you may wish to consider.
func NewStreamMemWithBuffer(BufferVar []byte, LenVar uint) *StreamMem {
	var cls *StreamMem

	cret := xNewStreamMemWithBuffer(BufferVar, LenVar)

	if cret == 0 {
		return nil
	}
	cls = &StreamMem{}
	cls.Ptr = cret
	return cls
}

var xNewStreamMemWithByteArray func([]byte) uintptr

// Create a new #CamelStreamMem using @buffer as the stream data.
//
// Note: The newly created #CamelStreamMem will destroy @buffer
// when destroyed.
func NewStreamMemWithByteArray(BufferVar []byte) *StreamMem {
	var cls *StreamMem

	cret := xNewStreamMemWithByteArray(BufferVar)

	if cret == 0 {
		return nil
	}
	cls = &StreamMem{}
	cls.Ptr = cret
	return cls
}

var xStreamMemGetByteArray func(uintptr) uintptr

func (x *StreamMem) GetByteArray() uintptr {

	cret := xStreamMemGetByteArray(x.GoPointer())
	return cret
}

var xStreamMemSetBuffer func(uintptr, []byte, uint)

// Set @buffer to be the backing data to the existing #CamelStreamMem, @mem.
//
// Note: @buffer will be copied into an internal #GByteArray structure
// and so may have resource implications to consider.
func (x *StreamMem) SetBuffer(BufferVar []byte, LenVar uint) {

	xStreamMemSetBuffer(x.GoPointer(), BufferVar, LenVar)

}

var xStreamMemSetByteArray func(uintptr, []byte)

// Set @buffer to be the backing data to the existing #CamelStreamMem, @mem.
//
// Note: @mem will not take ownership of @buffer and so will need to
// be freed separately from @mem.
func (x *StreamMem) SetByteArray(BufferVar []byte) {

	xStreamMemSetByteArray(x.GoPointer(), BufferVar)

}

var xStreamMemSetSecure func(uintptr)

// Mark the memory stream as secure.  At the very least this means the
// data in the buffer will be cleared when the buffer is finalized.
// This only applies to buffers owned by the stream.
func (x *StreamMem) SetSecure() {

	xStreamMemSetSecure(x.GoPointer())

}

func (c *StreamMem) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *StreamMem) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// Tests if the stream supports the #GSeekableIface.
func (x *StreamMem) CanSeek() bool {

	cret := gio.XGSeekableCanSeek(x.GoPointer())
	return cret
}

// Tests if the length of the stream can be adjusted with
// g_seekable_truncate().
func (x *StreamMem) CanTruncate() bool {

	cret := gio.XGSeekableCanTruncate(x.GoPointer())
	return cret
}

// Seeks in the stream by the given @offset, modified by @type.
//
// Attempting to seek past the end of the stream will have different
// results depending on if the stream is fixed-sized or resizable.  If
// the stream is resizable then seeking past the end and then writing
// will result in zeros filling the empty space.  Seeking past the end
// of a resizable stream and reading will result in EOF.  Seeking past
// the end of a fixed-sized stream will fail.
//
// Any operation that would result in a negative offset will fail.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned.
func (x *StreamMem) Seek(OffsetVar int64, TypeVar glib.SeekType, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableSeek(x.GoPointer(), OffsetVar, TypeVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Tells the current position within the stream.
func (x *StreamMem) Tell() int64 {

	cret := gio.XGSeekableTell(x.GoPointer())
	return cret
}

// Sets the length of the stream to @offset. If the stream was previously
// larger than @offset, the extra data is discarded. If the stream was
// previously shorter than @offset, it is extended with NUL ('\0') bytes.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned. If an
// operation was partially finished when the operation was cancelled the
// partial result will be returned, without an error.
func (x *StreamMem) Truncate(OffsetVar int64, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableTruncate(x.GoPointer(), OffsetVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xStreamMemGLibType, libs, "camel_stream_mem_get_type")

	core.PuregoSafeRegister(&xNewStreamMem, libs, "camel_stream_mem_new")
	core.PuregoSafeRegister(&xNewStreamMemWithBuffer, libs, "camel_stream_mem_new_with_buffer")
	core.PuregoSafeRegister(&xNewStreamMemWithByteArray, libs, "camel_stream_mem_new_with_byte_array")

	core.PuregoSafeRegister(&xStreamMemGetByteArray, libs, "camel_stream_mem_get_byte_array")
	core.PuregoSafeRegister(&xStreamMemSetBuffer, libs, "camel_stream_mem_set_buffer")
	core.PuregoSafeRegister(&xStreamMemSetByteArray, libs, "camel_stream_mem_set_byte_array")
	core.PuregoSafeRegister(&xStreamMemSetSecure, libs, "camel_stream_mem_set_secure")

}

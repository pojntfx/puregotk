// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
)

type MemPool struct {
	_ structs.HostLayout
}

func (x *MemPool) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type MemPoolFlags int

const (

	// Allocate to native structure alignment
	MempoolAlignStructValue MemPoolFlags = 0
	// Allocate to words - 16 bit alignment
	MempoolAlignWordValue MemPoolFlags = 1
	// Allocate to bytes - 8 bit alignment
	MempoolAlignByteValue MemPoolFlags = 2
	// Which bits determine the alignment information
	MempoolAlignMaskValue MemPoolFlags = 3
)

var xMempoolAlloc func(*MemPool, int) uintptr

// Allocate a new data block in the mempool.  Size will
// be rounded up to the mempool's alignment restrictions
// before being used.
func MempoolAlloc(PoolVar *MemPool, SizeVar int) uintptr {

	cret := xMempoolAlloc(PoolVar, SizeVar)
	return cret
}

var xMempoolDestroy func(*MemPool)

// Free all memory associated with a mempool.
func MempoolDestroy(PoolVar *MemPool) {

	xMempoolDestroy(PoolVar)

}

var xMempoolFlush func(*MemPool, int)

// Flush used memory and mark allocated blocks as free.
//
// If @freeall is %TRUE, then all allocated blocks are free'd
// as well.  Otherwise only blocks above the threshold are
// actually freed, and the others are simply marked as empty.
func MempoolFlush(PoolVar *MemPool, FreeallVar int) {

	xMempoolFlush(PoolVar, FreeallVar)

}

var xMempoolNew func(int, int, MemPoolFlags) *MemPool

// Create a new mempool header.  Mempools can be used to efficiently
// allocate data which can then be freed as a whole.
//
// Mempools can also be used to efficiently allocate arbitrarily
// aligned data (such as strings) without incurring the space overhead
// of aligning each allocation (which is not required for strings).
//
// However, each allocation cannot be freed individually, only all
// or nothing.
func MempoolNew(BlocksizeVar int, ThresholdVar int, FlagsVar MemPoolFlags) *MemPool {

	cret := xMempoolNew(BlocksizeVar, ThresholdVar, FlagsVar)
	return cret
}

var xMempoolStrdup func(*MemPool, string) string

func MempoolStrdup(PoolVar *MemPool, StrVar string) string {

	cret := xMempoolStrdup(PoolVar, StrVar)
	return cret
}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xMempoolAlloc, libs, "camel_mempool_alloc")
	core.PuregoSafeRegister(&xMempoolDestroy, libs, "camel_mempool_destroy")
	core.PuregoSafeRegister(&xMempoolFlush, libs, "camel_mempool_flush")
	core.PuregoSafeRegister(&xMempoolNew, libs, "camel_mempool_new")
	core.PuregoSafeRegister(&xMempoolStrdup, libs, "camel_mempool_strdup")

}

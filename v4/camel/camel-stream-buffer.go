// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type StreamBufferClassGType struct {
	_ structs.HostLayout

	ParentClass StreamClassGType

	xInit uintptr

	xInitVbuf uintptr

	Reserved [20]uintptr
}

func (x *StreamBufferClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

// OverrideInit sets the "init" callback function.
func (x *StreamBufferClassGType) OverrideInit(cb func(*StreamBuffer, *Stream, StreamBufferMode)) {
	if cb == nil {
		x.xInit = 0
	} else {
		x.xInit = purego.NewCallback(func(StreamBufferVarp uintptr, StreamVarp uintptr, ModeVarp StreamBufferMode) {
			cb(StreamBufferNewFromInternalPtr(StreamBufferVarp), StreamNewFromInternalPtr(StreamVarp), ModeVarp)
		})
	}
}

// GetInit gets the "init" callback function.
func (x *StreamBufferClassGType) GetInit() func(*StreamBuffer, *Stream, StreamBufferMode) {
	if x.xInit == 0 {
		return nil
	}
	var rawCallback func(StreamBufferVarp uintptr, StreamVarp uintptr, ModeVarp StreamBufferMode)
	purego.RegisterFunc(&rawCallback, x.xInit)
	return func(StreamBufferVar *StreamBuffer, StreamVar *Stream, ModeVar StreamBufferMode) {
		rawCallback(StreamBufferVar.GoPointer(), StreamVar.GoPointer(), ModeVar)
	}
}

// OverrideInitVbuf sets the "init_vbuf" callback function.
func (x *StreamBufferClassGType) OverrideInitVbuf(cb func(*StreamBuffer, *Stream, StreamBufferMode, string, uint32)) {
	if cb == nil {
		x.xInitVbuf = 0
	} else {
		x.xInitVbuf = purego.NewCallback(func(StreamBufferVarp uintptr, StreamVarp uintptr, ModeVarp StreamBufferMode, BufVarp string, SizeVarp uint32) {
			cb(StreamBufferNewFromInternalPtr(StreamBufferVarp), StreamNewFromInternalPtr(StreamVarp), ModeVarp, BufVarp, SizeVarp)
		})
	}
}

// GetInitVbuf gets the "init_vbuf" callback function.
func (x *StreamBufferClassGType) GetInitVbuf() func(*StreamBuffer, *Stream, StreamBufferMode, string, uint32) {
	if x.xInitVbuf == 0 {
		return nil
	}
	var rawCallback func(StreamBufferVarp uintptr, StreamVarp uintptr, ModeVarp StreamBufferMode, BufVarp string, SizeVarp uint32)
	purego.RegisterFunc(&rawCallback, x.xInitVbuf)
	return func(StreamBufferVar *StreamBuffer, StreamVar *Stream, ModeVar StreamBufferMode, BufVar string, SizeVar uint32) {
		rawCallback(StreamBufferVar.GoPointer(), StreamVar.GoPointer(), ModeVar, BufVar, SizeVar)
	}
}

type StreamBufferPrivate struct {
	_ structs.HostLayout
}

func (x *StreamBufferPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamBufferMode int

const (
	StreamBufferBufferValue StreamBufferMode = 0

	StreamBufferNoneValue StreamBufferMode = 1

	StreamBufferReadValue StreamBufferMode = 0

	StreamBufferWriteValue StreamBufferMode = 128

	StreamBufferModeValue StreamBufferMode = 128
)

type StreamBuffer struct {
	Stream
}

var xStreamBufferGLibType func() types.GType

func StreamBufferGLibType() types.GType {
	return xStreamBufferGLibType()
}

func StreamBufferNewFromInternalPtr(ptr uintptr) *StreamBuffer {
	cls := &StreamBuffer{}
	cls.Ptr = ptr
	return cls
}

var xNewStreamBuffer func(uintptr, StreamBufferMode) uintptr

// Create a new buffered stream of another stream.  A default
// buffer size (1024 bytes), automatically managed will be used
// for buffering.
//
// The following values are available for @mode:
//
// #CAMEL_STREAM_BUFFER_BUFFER, Buffer the input/output in blocks.
// #CAMEL_STREAM_BUFFER_NEWLINE, Buffer on newlines (for output).
// #CAMEL_STREAM_BUFFER_NONE, Perform no buffering.
//
// Note that currently this is ignored and #CAMEL_STREAM_BUFFER_BUFFER
// is always used.
//
// In addition, one of the following mode options should be or'd
// together with the buffering mode:
//
// #CAMEL_STREAM_BUFFER_WRITE, Buffer in write mode.
// #CAMEL_STREAM_BUFFER_READ, Buffer in read mode.
//
// Buffering can only be done in one direction for any
// buffer instance.
func NewStreamBuffer(StreamVar *Stream, ModeVar StreamBufferMode) *StreamBuffer {
	var cls *StreamBuffer

	cret := xNewStreamBuffer(StreamVar.GoPointer(), ModeVar)

	if cret == 0 {
		return nil
	}
	cls = &StreamBuffer{}
	cls.Ptr = cret
	return cls
}

var xStreamBufferDiscardCache func(uintptr)

// Discards any cached data in the @sbf. The next read reads
// from the stream.
func (x *StreamBuffer) DiscardCache() {

	xStreamBufferDiscardCache(x.GoPointer())

}

var xStreamBufferGets func(uintptr, []byte, uint, uintptr, **glib.Error) int

// Read a line of characters up to the next newline character or
// @max-1 characters.
//
// If the newline character is encountered, then it will be
// included in the buffer @buf.  The buffer will be %NULL terminated.
func (x *StreamBuffer) Gets(BufVar []byte, MaxVar uint, CancellableVar *gio.Cancellable) (int, error) {
	var cerr *glib.Error

	cret := xStreamBufferGets(x.GoPointer(), BufVar, MaxVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xStreamBufferReadLine func(uintptr, uintptr, **glib.Error) string

// This function reads a complete newline-terminated line from the stream
// and returns it in allocated memory. The trailing newline (and carriage
// return if any) are not included in the returned string.
func (x *StreamBuffer) ReadLine(CancellableVar *gio.Cancellable) (string, error) {
	var cerr *glib.Error

	cret := xStreamBufferReadLine(x.GoPointer(), CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func (c *StreamBuffer) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *StreamBuffer) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// Tests if the stream supports the #GSeekableIface.
func (x *StreamBuffer) CanSeek() bool {

	cret := gio.XGSeekableCanSeek(x.GoPointer())
	return cret
}

// Tests if the length of the stream can be adjusted with
// g_seekable_truncate().
func (x *StreamBuffer) CanTruncate() bool {

	cret := gio.XGSeekableCanTruncate(x.GoPointer())
	return cret
}

// Seeks in the stream by the given @offset, modified by @type.
//
// Attempting to seek past the end of the stream will have different
// results depending on if the stream is fixed-sized or resizable.  If
// the stream is resizable then seeking past the end and then writing
// will result in zeros filling the empty space.  Seeking past the end
// of a resizable stream and reading will result in EOF.  Seeking past
// the end of a fixed-sized stream will fail.
//
// Any operation that would result in a negative offset will fail.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned.
func (x *StreamBuffer) Seek(OffsetVar int64, TypeVar glib.SeekType, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableSeek(x.GoPointer(), OffsetVar, TypeVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Tells the current position within the stream.
func (x *StreamBuffer) Tell() int64 {

	cret := gio.XGSeekableTell(x.GoPointer())
	return cret
}

// Sets the length of the stream to @offset. If the stream was previously
// larger than @offset, the extra data is discarded. If the stream was
// previously shorter than @offset, it is extended with NUL ('\0') bytes.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned. If an
// operation was partially finished when the operation was cancelled the
// partial result will be returned, without an error.
func (x *StreamBuffer) Truncate(OffsetVar int64, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableTruncate(x.GoPointer(), OffsetVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xStreamBufferGLibType, libs, "camel_stream_buffer_get_type")

	core.PuregoSafeRegister(&xNewStreamBuffer, libs, "camel_stream_buffer_new")

	core.PuregoSafeRegister(&xStreamBufferDiscardCache, libs, "camel_stream_buffer_discard_cache")
	core.PuregoSafeRegister(&xStreamBufferGets, libs, "camel_stream_buffer_gets")
	core.PuregoSafeRegister(&xStreamBufferReadLine, libs, "camel_stream_buffer_read_line")

}

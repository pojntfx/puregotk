// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type StreamFilterClassGType struct {
	_ structs.HostLayout

	ParentClass StreamClassGType

	Reserved [20]uintptr
}

func (x *StreamFilterClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamFilterPrivate struct {
	_ structs.HostLayout
}

func (x *StreamFilterPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamFilter struct {
	Stream
}

var xStreamFilterGLibType func() types.GType

func StreamFilterGLibType() types.GType {
	return xStreamFilterGLibType()
}

func StreamFilterNewFromInternalPtr(ptr uintptr) *StreamFilter {
	cls := &StreamFilter{}
	cls.Ptr = ptr
	return cls
}

var xNewStreamFilter func(uintptr) uintptr

// Create a new #CamelStreamFilter object. The @source stream
// is referenced, thus the caller can unref it, if not needed.
func NewStreamFilter(SourceVar *Stream) *StreamFilter {
	var cls *StreamFilter

	cret := xNewStreamFilter(SourceVar.GoPointer())

	if cret == 0 {
		return nil
	}
	cls = &StreamFilter{}
	cls.Ptr = cret
	return cls
}

var xStreamFilterAdd func(uintptr, uintptr) int

// Add a new #CamelMimeFilter to execute during the processing of this
// stream.  Each filter added is processed after the previous one.
//
// Note that a filter should only be added to a single stream
// at a time, otherwise unpredictable results may occur.
func (x *StreamFilter) Add(FilterVar *MimeFilter) int {

	cret := xStreamFilterAdd(x.GoPointer(), FilterVar.GoPointer())
	return cret
}

var xStreamFilterGetSource func(uintptr) uintptr

func (x *StreamFilter) GetSource() *Stream {
	var cls *Stream

	cret := xStreamFilterGetSource(x.GoPointer())

	if cret == 0 {
		return nil
	}
	gobject.IncreaseRef(cret)
	cls = &Stream{}
	cls.Ptr = cret
	return cls
}

var xStreamFilterRemove func(uintptr, int)

// Remove a processing filter from the stream by id.
func (x *StreamFilter) Remove(IdVar int) {

	xStreamFilterRemove(x.GoPointer(), IdVar)

}

func (c *StreamFilter) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *StreamFilter) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// Tests if the stream supports the #GSeekableIface.
func (x *StreamFilter) CanSeek() bool {

	cret := gio.XGSeekableCanSeek(x.GoPointer())
	return cret
}

// Tests if the length of the stream can be adjusted with
// g_seekable_truncate().
func (x *StreamFilter) CanTruncate() bool {

	cret := gio.XGSeekableCanTruncate(x.GoPointer())
	return cret
}

// Seeks in the stream by the given @offset, modified by @type.
//
// Attempting to seek past the end of the stream will have different
// results depending on if the stream is fixed-sized or resizable.  If
// the stream is resizable then seeking past the end and then writing
// will result in zeros filling the empty space.  Seeking past the end
// of a resizable stream and reading will result in EOF.  Seeking past
// the end of a fixed-sized stream will fail.
//
// Any operation that would result in a negative offset will fail.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned.
func (x *StreamFilter) Seek(OffsetVar int64, TypeVar glib.SeekType, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableSeek(x.GoPointer(), OffsetVar, TypeVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Tells the current position within the stream.
func (x *StreamFilter) Tell() int64 {

	cret := gio.XGSeekableTell(x.GoPointer())
	return cret
}

// Sets the length of the stream to @offset. If the stream was previously
// larger than @offset, the extra data is discarded. If the stream was
// previously shorter than @offset, it is extended with NUL ('\0') bytes.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned. If an
// operation was partially finished when the operation was cancelled the
// partial result will be returned, without an error.
func (x *StreamFilter) Truncate(OffsetVar int64, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableTruncate(x.GoPointer(), OffsetVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xStreamFilterGLibType, libs, "camel_stream_filter_get_type")

	core.PuregoSafeRegister(&xNewStreamFilter, libs, "camel_stream_filter_new")

	core.PuregoSafeRegister(&xStreamFilterAdd, libs, "camel_stream_filter_add")
	core.PuregoSafeRegister(&xStreamFilterGetSource, libs, "camel_stream_filter_get_source")
	core.PuregoSafeRegister(&xStreamFilterRemove, libs, "camel_stream_filter_remove")

}

// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type OperationClassGType struct {
	_ structs.HostLayout

	ParentClass gio.CancellableClassGType

	xStatus uintptr

	Reserved [20]uintptr
}

func (x *OperationClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

// OverrideStatus sets the "status" callback function.
func (x *OperationClassGType) OverrideStatus(cb func(*Operation, string, int)) {
	if cb == nil {
		x.xStatus = 0
	} else {
		x.xStatus = purego.NewCallback(func(OperationVarp uintptr, WhatVarp string, PcVarp int) {
			cb(OperationNewFromInternalPtr(OperationVarp), WhatVarp, PcVarp)
		})
	}
}

// GetStatus gets the "status" callback function.
func (x *OperationClassGType) GetStatus() func(*Operation, string, int) {
	if x.xStatus == 0 {
		return nil
	}
	var rawCallback func(OperationVarp uintptr, WhatVarp string, PcVarp int)
	purego.RegisterFunc(&rawCallback, x.xStatus)
	return func(OperationVar *Operation, WhatVar string, PcVar int) {
		rawCallback(OperationVar.GoPointer(), WhatVar, PcVar)
	}
}

type OperationPrivate struct {
	_ structs.HostLayout
}

func (x *OperationPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type Operation struct {
	gio.Cancellable
}

var xOperationGLibType func() types.GType

func OperationGLibType() types.GType {
	return xOperationGLibType()
}

func OperationNewFromInternalPtr(ptr uintptr) *Operation {
	cls := &Operation{}
	cls.Ptr = ptr
	return cls
}

var xNewOperation func() uintptr

// Create a new camel operation handle.  Camel operation handles can
// be used in a multithreaded application (or a single operation
// handle can be used in a non threaded appliation) to cancel running
// operations and to obtain notification messages of the internal
// status of messages.
func NewOperation() *Operation {
	var cls *Operation

	cret := xNewOperation()

	if cret == 0 {
		return nil
	}
	cls = &Operation{}
	cls.Ptr = cret
	return cls
}

var xNewOperationProxy func(uintptr) uintptr

// Proxies the @cancellable in a way that if it is cancelled,
// then the returned cancellable is also cancelled, but when
// the returned cancellable is cancelled, then it doesn't
// influence the original cancellable. Other CamelOperation
// actions being done on the returned cancellable are also
// propagated to the @cancellable.
//
// The passed-in @cancellable can be %NULL, in which case
// a plain CamelOperation is returned.
//
// This is useful when some operation can be cancelled from
// elsewhere (like by a user), but also by the code on its own,
// when it doesn't make sense to cancel also any larger operation
// to which the passed-in cancellable belongs.
func NewOperationProxy(CancellableVar *gio.Cancellable) *Operation {
	var cls *Operation

	cret := xNewOperationProxy(CancellableVar.GoPointer())

	if cret == 0 {
		return nil
	}
	cls = &Operation{}
	cls.Ptr = cret
	return cls
}

func (c *Operation) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *Operation) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

func (x *Operation) ConnectPopMessage(cb *func(Operation)) uint32 {
	cbPtr := uintptr(unsafe.Pointer(cb))
	if cbRefPtr, ok := glib.GetCallback(cbPtr); ok {
		return gobject.SignalConnect(x.GoPointer(), "pop-message", cbRefPtr)
	}

	fcb := func(clsPtr uintptr) {
		fa := Operation{}
		fa.Ptr = clsPtr
		cbFn := *cb

		cbFn(fa)

	}
	cbRefPtr := purego.NewCallback(fcb)
	glib.SaveCallback(cbPtr, cbRefPtr)
	return gobject.SignalConnect(x.GoPointer(), "pop-message", cbRefPtr)
}

func (x *Operation) ConnectProgress(cb *func(Operation, int)) uint32 {
	cbPtr := uintptr(unsafe.Pointer(cb))
	if cbRefPtr, ok := glib.GetCallback(cbPtr); ok {
		return gobject.SignalConnect(x.GoPointer(), "progress", cbRefPtr)
	}

	fcb := func(clsPtr uintptr, ObjectVarp int) {
		fa := Operation{}
		fa.Ptr = clsPtr
		cbFn := *cb

		cbFn(fa, ObjectVarp)

	}
	cbRefPtr := purego.NewCallback(fcb)
	glib.SaveCallback(cbPtr, cbRefPtr)
	return gobject.SignalConnect(x.GoPointer(), "progress", cbRefPtr)
}

func (x *Operation) ConnectPushMessage(cb *func(Operation, string)) uint32 {
	cbPtr := uintptr(unsafe.Pointer(cb))
	if cbRefPtr, ok := glib.GetCallback(cbPtr); ok {
		return gobject.SignalConnect(x.GoPointer(), "push-message", cbRefPtr)
	}

	fcb := func(clsPtr uintptr, ObjectVarp string) {
		fa := Operation{}
		fa.Ptr = clsPtr
		cbFn := *cb

		cbFn(fa, ObjectVarp)

	}
	cbRefPtr := purego.NewCallback(fcb)
	glib.SaveCallback(cbPtr, cbRefPtr)
	return gobject.SignalConnect(x.GoPointer(), "push-message", cbRefPtr)
}

func (x *Operation) ConnectStatus(cb *func(Operation, string, int)) uint32 {
	cbPtr := uintptr(unsafe.Pointer(cb))
	if cbRefPtr, ok := glib.GetCallback(cbPtr); ok {
		return gobject.SignalConnect(x.GoPointer(), "status", cbRefPtr)
	}

	fcb := func(clsPtr uintptr, ObjectVarp string, P0Varp int) {
		fa := Operation{}
		fa.Ptr = clsPtr
		cbFn := *cb

		cbFn(fa, ObjectVarp, P0Varp)

	}
	cbRefPtr := purego.NewCallback(fcb)
	glib.SaveCallback(cbPtr, cbRefPtr)
	return gobject.SignalConnect(x.GoPointer(), "status", cbRefPtr)
}

var xOperationCancelAll func()

// Cancel all outstanding operations.
func OperationCancelAll() {

	xOperationCancelAll()

}

var xOperationDupMessage func(uintptr) string

// Duplicates current operation message, or returns %NULL, if no such is available.
// The message as the last text set by camel_operation_push_message().
//
// Free the returned text with g_free(), when no longer needed.
//
// This function only works if @cancellable is a #CamelOperation cast as a
// #GCancellable.  If @cancellable is a plain #GCancellable or %NULL, the
// function does nothing and returns silently.
func OperationDupMessage(CancellableVar *gio.Cancellable) string {

	cret := xOperationDupMessage(CancellableVar.GoPointer())
	return cret
}

var xOperationPopMessage func(uintptr)

// Pops the most recently pushed message.
//
// This function only works if @cancellable is a #CamelOperation cast as a
// #GCancellable.  If @cancellable is a plain #GCancellable or %NULL, the
// function does nothing and returns silently.
func OperationPopMessage(CancellableVar *gio.Cancellable) {

	xOperationPopMessage(CancellableVar.GoPointer())

}

var xOperationProgress func(uintptr, int)

// Report progress on the current operation.  @percent reports the current
// percentage of completion, which should be in the range of 0 to 100.
//
// This function only works if @cancellable is a #CamelOperation cast as a
// #GCancellable.  If @cancellable is a plain #GCancellable or %NULL, the
// function does nothing and returns silently.
func OperationProgress(CancellableVar *gio.Cancellable, PercentVar int) {

	xOperationProgress(CancellableVar.GoPointer(), PercentVar)

}

var xOperationPushMessage func(uintptr, string, ...interface{})

// Call this function to describe an operation being performed.
// Call camel_operation_progress() to report progress on the operation.
// Call camel_operation_pop_message() when the operation is complete.
//
// This function only works if @cancellable is a #CamelOperation cast as a
// #GCancellable.  If @cancellable is a plain #GCancellable or %NULL, the
// function does nothing and returns silently.
func OperationPushMessage(CancellableVar *gio.Cancellable, FormatVar string, varArgs ...interface{}) {

	xOperationPushMessage(CancellableVar.GoPointer(), FormatVar, varArgs...)

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xOperationGLibType, libs, "camel_operation_get_type")

	core.PuregoSafeRegister(&xNewOperation, libs, "camel_operation_new")
	core.PuregoSafeRegister(&xNewOperationProxy, libs, "camel_operation_new_proxy")

	core.PuregoSafeRegister(&xOperationCancelAll, libs, "camel_operation_cancel_all")
	core.PuregoSafeRegister(&xOperationDupMessage, libs, "camel_operation_dup_message")
	core.PuregoSafeRegister(&xOperationPopMessage, libs, "camel_operation_pop_message")
	core.PuregoSafeRegister(&xOperationProgress, libs, "camel_operation_progress")
	core.PuregoSafeRegister(&xOperationPushMessage, libs, "camel_operation_push_message")

}

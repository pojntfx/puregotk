// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type OfflineStoreClassGType struct {
	_ structs.HostLayout

	ParentClass StoreClassGType

	xDupDownsyncFolders uintptr

	Reserved [19]uintptr
}

func (x *OfflineStoreClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

// OverrideDupDownsyncFolders sets the "dup_downsync_folders" callback function.
func (x *OfflineStoreClassGType) OverrideDupDownsyncFolders(cb func(*OfflineStore) uintptr) {
	if cb == nil {
		x.xDupDownsyncFolders = 0
	} else {
		x.xDupDownsyncFolders = purego.NewCallback(func(StoreVarp uintptr) uintptr {
			return cb(OfflineStoreNewFromInternalPtr(StoreVarp))
		})
	}
}

// GetDupDownsyncFolders gets the "dup_downsync_folders" callback function.
func (x *OfflineStoreClassGType) GetDupDownsyncFolders() func(*OfflineStore) uintptr {
	if x.xDupDownsyncFolders == 0 {
		return nil
	}
	var rawCallback func(StoreVarp uintptr) uintptr
	purego.RegisterFunc(&rawCallback, x.xDupDownsyncFolders)
	return func(StoreVar *OfflineStore) uintptr {
		return rawCallback(StoreVar.GoPointer())
	}
}

type OfflineStorePrivate struct {
	_ structs.HostLayout
}

func (x *OfflineStorePrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type OfflineStore struct {
	Store
}

var xOfflineStoreGLibType func() types.GType

func OfflineStoreGLibType() types.GType {
	return xOfflineStoreGLibType()
}

func OfflineStoreNewFromInternalPtr(ptr uintptr) *OfflineStore {
	cls := &OfflineStore{}
	cls.Ptr = ptr
	return cls
}

var xOfflineStoreDupDownsyncFolders func(uintptr) uintptr

// Returns a #GPtrArray of #CamelFolder objects which should be checked
// for offline synchronization. Free the returned pointer with the below
// calls, when no longer needed:
//
// |[
//
//	g_ptr_array_foreach (array, (GFunc) g_object_unref, NULL);
//	g_ptr_array_free (array, TRUE);
//
// ]|
func (x *OfflineStore) DupDownsyncFolders() uintptr {

	cret := xOfflineStoreDupDownsyncFolders(x.GoPointer())
	return cret
}

var xOfflineStoreGetOnline func(uintptr) bool

// Returns %TRUE if @store is online.
func (x *OfflineStore) GetOnline() bool {

	cret := xOfflineStoreGetOnline(x.GoPointer())
	return cret
}

var xOfflineStorePrepareForOfflineSync func(uintptr, uintptr, **glib.Error) bool

// Downloads messages for offline, when setup to do so and when
// the host is reachable.
func (x *OfflineStore) PrepareForOfflineSync(CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := xOfflineStorePrepareForOfflineSync(x.GoPointer(), CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xOfflineStoreRequiresDownsync func(uintptr) bool

// Check whether the @store requires synchronization for offline usage.
// This is not blocking, it only checks settings on the store and its
// currently opened folders.
//
// Returns %TRUE if the @store requires synchronization for offline usage
func (x *OfflineStore) RequiresDownsync() bool {

	cret := xOfflineStoreRequiresDownsync(x.GoPointer())
	return cret
}

var xOfflineStoreSetOnline func(uintptr, bool, int, uintptr, uintptr, uintptr)

// An asynchronous variant of camel_offline_store_set_online_sync().
// Call camel_offline_store_set_online_finish() from within the @callback.
func (x *OfflineStore) SetOnline(OnlineVar bool, IoPriorityVar int, CancellableVar *gio.Cancellable, CallbackVar *gio.AsyncReadyCallback, UserDataVar uintptr) {

	xOfflineStoreSetOnline(x.GoPointer(), OnlineVar, IoPriorityVar, CancellableVar.GoPointer(), glib.NewCallbackNullable(CallbackVar), UserDataVar)

}

var xOfflineStoreSetOnlineFinish func(uintptr, uintptr, **glib.Error) bool

// Finishes the operation started with camel_offline_store_set_online().
func (x *OfflineStore) SetOnlineFinish(ResultVar gio.AsyncResult) (bool, error) {
	var cerr *glib.Error

	cret := xOfflineStoreSetOnlineFinish(x.GoPointer(), ResultVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xOfflineStoreSetOnlineSync func(uintptr, bool, uintptr, **glib.Error) bool

// Sets the online/offline state of @store according to @online.
func (x *OfflineStore) SetOnlineSync(OnlineVar bool, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := xOfflineStoreSetOnlineSync(x.GoPointer(), OnlineVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func (c *OfflineStore) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *OfflineStore) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// GetPropertyOnline gets the "online" property.
// Whether the store is online
func (x *OfflineStore) GetPropertyOnline() bool {
	var v gobject.Value
	x.GetProperty("online", &v)
	return v.GetBoolean()
}

// Initializes the object implementing the interface.
//
// This method is intended for language bindings. If writing in C,
// g_initable_new() should typically be used instead.
//
// The object must be initialized before any real use after initial
// construction, either with this function or g_async_initable_init_async().
//
// Implementations may also support cancellation. If @cancellable is not %NULL,
// then initialization can be cancelled by triggering the cancellable object
// from another thread. If the operation was cancelled, the error
// %G_IO_ERROR_CANCELLED will be returned. If @cancellable is not %NULL and
// the object doesn't support cancellable initialization the error
// %G_IO_ERROR_NOT_SUPPORTED will be returned.
//
// If the object is not initialized, or initialization returns with an
// error, then all operations on the object except g_object_ref() and
// g_object_unref() are considered to be invalid, and have undefined
// behaviour. See the [description][iface@Gio.Initable#description] for more details.
//
// Callers should not assume that a class which implements #GInitable can be
// initialized multiple times, unless the class explicitly documents itself as
// supporting this. Generally, a class’ implementation of init() can assume
// (and assert) that it will only be called once. Previously, this documentation
// recommended all #GInitable implementations should be idempotent; that
// recommendation was relaxed in GLib 2.54.
//
// If a class explicitly supports being initialized multiple times, it is
// recommended that the method is idempotent: multiple calls with the same
// arguments should return the same results. Only the first call initializes
// the object; further calls return the result of the first call.
//
// One reason why a class might need to support idempotent initialization is if
// it is designed to be used via the singleton pattern, with a
// #GObjectClass.constructor that sometimes returns an existing instance.
// In this pattern, a caller would expect to be able to call g_initable_init()
// on the result of g_object_new(), regardless of whether it is in fact a new
// instance.
func (x *OfflineStore) Init(CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGInitableInit(x.GoPointer(), CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xOfflineStoreGLibType, libs, "camel_offline_store_get_type")

	core.PuregoSafeRegister(&xOfflineStoreDupDownsyncFolders, libs, "camel_offline_store_dup_downsync_folders")
	core.PuregoSafeRegister(&xOfflineStoreGetOnline, libs, "camel_offline_store_get_online")
	core.PuregoSafeRegister(&xOfflineStorePrepareForOfflineSync, libs, "camel_offline_store_prepare_for_offline_sync")
	core.PuregoSafeRegister(&xOfflineStoreRequiresDownsync, libs, "camel_offline_store_requires_downsync")
	core.PuregoSafeRegister(&xOfflineStoreSetOnline, libs, "camel_offline_store_set_online")
	core.PuregoSafeRegister(&xOfflineStoreSetOnlineFinish, libs, "camel_offline_store_set_online_finish")
	core.PuregoSafeRegister(&xOfflineStoreSetOnlineSync, libs, "camel_offline_store_set_online_sync")

}

// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type StreamProcessClassGType struct {
	_ structs.HostLayout

	ParentClass StreamClassGType

	Reserved [20]uintptr
}

func (x *StreamProcessClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamProcessPrivate struct {
	_ structs.HostLayout
}

func (x *StreamProcessPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamProcess struct {
	Stream
}

var xStreamProcessGLibType func() types.GType

func StreamProcessGLibType() types.GType {
	return xStreamProcessGLibType()
}

func StreamProcessNewFromInternalPtr(ptr uintptr) *StreamProcess {
	cls := &StreamProcess{}
	cls.Ptr = ptr
	return cls
}

var xNewStreamProcess func() uintptr

// Returns a PROCESS stream.
func NewStreamProcess() *StreamProcess {
	var cls *StreamProcess

	cret := xNewStreamProcess()

	if cret == 0 {
		return nil
	}
	cls = &StreamProcess{}
	cls.Ptr = cret
	return cls
}

var xStreamProcessConnect func(uintptr, string, string, **glib.Error) int

func (x *StreamProcess) Connect(CommandVar string, EnvVar string) (int, error) {
	var cerr *glib.Error

	cret := xStreamProcessConnect(x.GoPointer(), CommandVar, EnvVar, &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func (c *StreamProcess) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *StreamProcess) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// Tests if the stream supports the #GSeekableIface.
func (x *StreamProcess) CanSeek() bool {

	cret := gio.XGSeekableCanSeek(x.GoPointer())
	return cret
}

// Tests if the length of the stream can be adjusted with
// g_seekable_truncate().
func (x *StreamProcess) CanTruncate() bool {

	cret := gio.XGSeekableCanTruncate(x.GoPointer())
	return cret
}

// Seeks in the stream by the given @offset, modified by @type.
//
// Attempting to seek past the end of the stream will have different
// results depending on if the stream is fixed-sized or resizable.  If
// the stream is resizable then seeking past the end and then writing
// will result in zeros filling the empty space.  Seeking past the end
// of a resizable stream and reading will result in EOF.  Seeking past
// the end of a fixed-sized stream will fail.
//
// Any operation that would result in a negative offset will fail.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned.
func (x *StreamProcess) Seek(OffsetVar int64, TypeVar glib.SeekType, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableSeek(x.GoPointer(), OffsetVar, TypeVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Tells the current position within the stream.
func (x *StreamProcess) Tell() int64 {

	cret := gio.XGSeekableTell(x.GoPointer())
	return cret
}

// Sets the length of the stream to @offset. If the stream was previously
// larger than @offset, the extra data is discarded. If the stream was
// previously shorter than @offset, it is extended with NUL ('\0') bytes.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned. If an
// operation was partially finished when the operation was cancelled the
// partial result will be returned, without an error.
func (x *StreamProcess) Truncate(OffsetVar int64, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableTruncate(x.GoPointer(), OffsetVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xStreamProcessGLibType, libs, "camel_stream_process_get_type")

	core.PuregoSafeRegister(&xNewStreamProcess, libs, "camel_stream_process_new")

	core.PuregoSafeRegister(&xStreamProcessConnect, libs, "camel_stream_process_connect")

}

// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type TransportClassGType struct {
	_ structs.HostLayout

	ParentClass ServiceClassGType

	xSendToSync uintptr

	Reserved [20]uintptr
}

func (x *TransportClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

// OverrideSendToSync sets the "send_to_sync" callback function.
func (x *TransportClassGType) OverrideSendToSync(cb func(*Transport, *MimeMessage, *Address, *Address, *bool, *gio.Cancellable) bool) {
	if cb == nil {
		x.xSendToSync = 0
	} else {
		x.xSendToSync = purego.NewCallback(func(TransportVarp uintptr, MessageVarp uintptr, FromVarp uintptr, RecipientsVarp uintptr, OutSentMessageSavedVarp *bool, CancellableVarp uintptr) bool {
			return cb(TransportNewFromInternalPtr(TransportVarp), MimeMessageNewFromInternalPtr(MessageVarp), AddressNewFromInternalPtr(FromVarp), AddressNewFromInternalPtr(RecipientsVarp), OutSentMessageSavedVarp, gio.CancellableNewFromInternalPtr(CancellableVarp))
		})
	}
}

// GetSendToSync gets the "send_to_sync" callback function.
func (x *TransportClassGType) GetSendToSync() func(*Transport, *MimeMessage, *Address, *Address, *bool, *gio.Cancellable) bool {
	if x.xSendToSync == 0 {
		return nil
	}
	var rawCallback func(TransportVarp uintptr, MessageVarp uintptr, FromVarp uintptr, RecipientsVarp uintptr, OutSentMessageSavedVarp *bool, CancellableVarp uintptr) bool
	purego.RegisterFunc(&rawCallback, x.xSendToSync)
	return func(TransportVar *Transport, MessageVar *MimeMessage, FromVar *Address, RecipientsVar *Address, OutSentMessageSavedVar *bool, CancellableVar *gio.Cancellable) bool {
		return rawCallback(TransportVar.GoPointer(), MessageVar.GoPointer(), FromVar.GoPointer(), RecipientsVar.GoPointer(), OutSentMessageSavedVar, CancellableVar.GoPointer())
	}
}

type TransportPrivate struct {
	_ structs.HostLayout
}

func (x *TransportPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type Transport struct {
	Service
}

var xTransportGLibType func() types.GType

func TransportGLibType() types.GType {
	return xTransportGLibType()
}

func TransportNewFromInternalPtr(ptr uintptr) *Transport {
	cls := &Transport{}
	cls.Ptr = ptr
	return cls
}

var xTransportGetRequestDsn func(uintptr) bool

// Returns whether should request Delivery Status Notification
// in the "send_to" operation.
func (x *Transport) GetRequestDsn() bool {

	cret := xTransportGetRequestDsn(x.GoPointer())
	return cret
}

var xTransportSendTo func(uintptr, uintptr, uintptr, uintptr, int, uintptr, uintptr, uintptr)

// Sends the message asynchronously to the given recipients, regardless of
// the contents of @message.  If the message contains a "Bcc" header, the
// transport is responsible for stripping it.
//
// When the operation is finished, @callback will be called.  You can then
// call camel_transport_send_to_finish() to get the result of the operation.
func (x *Transport) SendTo(MessageVar *MimeMessage, FromVar *Address, RecipientsVar *Address, IoPriorityVar int, CancellableVar *gio.Cancellable, CallbackVar *gio.AsyncReadyCallback, UserDataVar uintptr) {

	xTransportSendTo(x.GoPointer(), MessageVar.GoPointer(), FromVar.GoPointer(), RecipientsVar.GoPointer(), IoPriorityVar, CancellableVar.GoPointer(), glib.NewCallbackNullable(CallbackVar), UserDataVar)

}

var xTransportSendToFinish func(uintptr, uintptr, *bool, **glib.Error) bool

// Finishes the operation started with camel_transport_send_to().
func (x *Transport) SendToFinish(ResultVar gio.AsyncResult, OutSentMessageSavedVar *bool) (bool, error) {
	var cerr *glib.Error

	cret := xTransportSendToFinish(x.GoPointer(), ResultVar.GoPointer(), OutSentMessageSavedVar, &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xTransportSendToSync func(uintptr, uintptr, uintptr, uintptr, *bool, uintptr, **glib.Error) bool

// Sends the message to the given recipients, regardless of the contents
// of @message.  If the message contains a "Bcc" header, the transport
// is responsible for stripping it.
func (x *Transport) SendToSync(MessageVar *MimeMessage, FromVar *Address, RecipientsVar *Address, OutSentMessageSavedVar *bool, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := xTransportSendToSync(x.GoPointer(), MessageVar.GoPointer(), FromVar.GoPointer(), RecipientsVar.GoPointer(), OutSentMessageSavedVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xTransportSetRequestDsn func(uintptr, bool)

// Sets whether should request Delivery Status Notification
// during the "send_to" operation.
func (x *Transport) SetRequestDsn(RequestDsnVar bool) {

	xTransportSetRequestDsn(x.GoPointer(), RequestDsnVar)

}

func (c *Transport) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *Transport) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// Initializes the object implementing the interface.
//
// This method is intended for language bindings. If writing in C,
// g_initable_new() should typically be used instead.
//
// The object must be initialized before any real use after initial
// construction, either with this function or g_async_initable_init_async().
//
// Implementations may also support cancellation. If @cancellable is not %NULL,
// then initialization can be cancelled by triggering the cancellable object
// from another thread. If the operation was cancelled, the error
// %G_IO_ERROR_CANCELLED will be returned. If @cancellable is not %NULL and
// the object doesn't support cancellable initialization the error
// %G_IO_ERROR_NOT_SUPPORTED will be returned.
//
// If the object is not initialized, or initialization returns with an
// error, then all operations on the object except g_object_ref() and
// g_object_unref() are considered to be invalid, and have undefined
// behaviour. See the [description][iface@Gio.Initable#description] for more details.
//
// Callers should not assume that a class which implements #GInitable can be
// initialized multiple times, unless the class explicitly documents itself as
// supporting this. Generally, a class’ implementation of init() can assume
// (and assert) that it will only be called once. Previously, this documentation
// recommended all #GInitable implementations should be idempotent; that
// recommendation was relaxed in GLib 2.54.
//
// If a class explicitly supports being initialized multiple times, it is
// recommended that the method is idempotent: multiple calls with the same
// arguments should return the same results. Only the first call initializes
// the object; further calls return the result of the first call.
//
// One reason why a class might need to support idempotent initialization is if
// it is designed to be used via the singleton pattern, with a
// #GObjectClass.constructor that sometimes returns an existing instance.
// In this pattern, a caller would expect to be able to call g_initable_init()
// on the result of g_object_new(), regardless of whether it is in fact a new
// instance.
func (x *Transport) Init(CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGInitableInit(x.GoPointer(), CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xTransportGLibType, libs, "camel_transport_get_type")

	core.PuregoSafeRegister(&xTransportGetRequestDsn, libs, "camel_transport_get_request_dsn")
	core.PuregoSafeRegister(&xTransportSendTo, libs, "camel_transport_send_to")
	core.PuregoSafeRegister(&xTransportSendToFinish, libs, "camel_transport_send_to_finish")
	core.PuregoSafeRegister(&xTransportSendToSync, libs, "camel_transport_send_to_sync")
	core.PuregoSafeRegister(&xTransportSetRequestDsn, libs, "camel_transport_set_request_dsn")

}

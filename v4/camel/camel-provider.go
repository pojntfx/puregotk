// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type Provider struct {
	_ structs.HostLayout

	Protocol uintptr

	Name uintptr

	Description uintptr

	Domain uintptr

	Flags ProviderFlags

	UrlFlags ProviderURLFlags

	ExtraConf *ProviderConfEntry

	PortEntries *ProviderPortEntry

	ObjectTypes []types.GType

	Authtypes *glib.List

	UrlHash glib.HashFunc

	UrlEqual glib.EqualFunc

	TranslationDomain uintptr

	Priv uintptr
}

var xProviderGLibType func() types.GType

func ProviderGLibType() types.GType {
	return xProviderGLibType()
}

func (x *Provider) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

var xProviderRegister func(uintptr)

// Registers a provider.
func (x *Provider) Register() {

	xProviderRegister(x.GoPointer())

}

type ProviderConfEntry struct {
	_ structs.HostLayout

	Type ProviderConfType

	Name uintptr

	Depname uintptr

	Text uintptr

	Value uintptr
}

func (x *ProviderConfEntry) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type ProviderModule struct {
	_ structs.HostLayout

	Path uintptr

	Types *glib.SList

	Loaded uint32
}

func (x *ProviderModule) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type ProviderPortEntry struct {
	_ structs.HostLayout

	Port int32

	Desc uintptr

	IsSsl bool
}

func (x *ProviderPortEntry) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

const (
	// This environment variable configures where the camel
	// provider modules are loaded from.
	EDS_CAMEL_PROVIDER_DIR string = "EDS_CAMEL_PROVIDER_DIR"

	URL_PART_AUTH int = 2

	URL_PART_HIDDEN int = 8

	URL_PART_HOST int = 8

	URL_PART_NEED int = 8

	URL_PART_PASSWORD int = 4

	URL_PART_PATH int = 32

	URL_PART_PATH_DIR int = 64

	URL_PART_PORT int = 16

	URL_PART_USER int = 1
)

type ProviderURLFlags int

const (
	UrlAllowUserValue ProviderURLFlags = 1

	UrlAllowAuthValue ProviderURLFlags = 2

	UrlAllowPasswordValue ProviderURLFlags = 4

	UrlAllowHostValue ProviderURLFlags = 8

	UrlAllowPortValue ProviderURLFlags = 16

	UrlAllowPathValue ProviderURLFlags = 32

	UrlNeedUserValue ProviderURLFlags = 256

	UrlNeedAuthValue ProviderURLFlags = 512

	UrlNeedPasswordValue ProviderURLFlags = 1024

	UrlNeedHostValue ProviderURLFlags = 2048

	UrlNeedPortValue ProviderURLFlags = 4096

	UrlNeedPathValue ProviderURLFlags = 8192

	UrlNeedPathDirValue ProviderURLFlags = 16384

	UrlHiddenUserValue ProviderURLFlags = 65536

	UrlHiddenAuthValue ProviderURLFlags = 131072

	UrlHiddenPasswordValue ProviderURLFlags = 262144

	UrlHiddenHostValue ProviderURLFlags = 524288

	UrlHiddenPortValue ProviderURLFlags = 1048576

	UrlHiddenPathValue ProviderURLFlags = 2097152

	UrlFragmentIsPathValue ProviderURLFlags = 1073741824

	UrlPathIsAbsoluteValue ProviderURLFlags = 2147483648
)

var xProviderGet func(string, **glib.Error) *Provider

// Returns the registered #CamelProvider for @protocol, loading it
// from disk if necessary.  If no #CamelProvider can be found for
// @protocol, or the provider module fails to load, the function
// sets @error and returns %NULL.
//
// The returned #CamelProvider is owned by Camel and should not be
// modified or freed.
func ProviderGet(ProtocolVar string) (*Provider, error) {
	var cerr *glib.Error

	cret := xProviderGet(ProtocolVar, &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xProviderInit func()

// Initialize the Camel provider system by reading in the .urls
// files in the provider directory and creating a hash table mapping
// URLs to module names.
//
// A .urls file has the same initial prefix as the shared library it
// correspond to, and consists of a series of lines containing the URL
// protocols that that library handles.
//
// TODO: This should be pathed?
// TODO: This should be plugin-d?
func ProviderInit() {

	xProviderInit()

}

var xProviderList func(bool) *glib.List

// This returns a list of available providers. If @load is %TRUE, it will
// first load in all available providers that haven't yet been loaded.
//
// Free the returned list with g_list_free().  The #CamelProvider structs
// in the list are owned by Camel and should not be modified or freed.
func ProviderList(LoadVar bool) *glib.List {

	cret := xProviderList(LoadVar)
	return cret
}

var xProviderLoad func(string, **glib.Error) bool

// Loads the provider at @path, and calls its initialization function,
// passing @session as an argument. The provider should then register
// itself with @session.
func ProviderLoad(PathVar string) (bool, error) {
	var cerr *glib.Error

	cret := xProviderLoad(PathVar, &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

var xProviderModuleInit func()

func ProviderModuleInit() {

	xProviderModuleInit()

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xProviderGet, libs, "camel_provider_get")
	core.PuregoSafeRegister(&xProviderInit, libs, "camel_provider_init")
	core.PuregoSafeRegister(&xProviderList, libs, "camel_provider_list")
	core.PuregoSafeRegister(&xProviderLoad, libs, "camel_provider_load")
	core.PuregoSafeRegister(&xProviderModuleInit, libs, "camel_provider_module_init")

	core.PuregoSafeRegister(&xProviderGLibType, libs, "camel_provider_get_type")

	core.PuregoSafeRegister(&xProviderRegister, libs, "camel_provider_register")

}

// Package camel was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package camel

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type StreamFsClassGType struct {
	_ structs.HostLayout

	ParentClass StreamClassGType

	Reserved [20]uintptr
}

func (x *StreamFsClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamFsPrivate struct {
	_ structs.HostLayout
}

func (x *StreamFsPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type StreamFs struct {
	Stream
}

var xStreamFsGLibType func() types.GType

func StreamFsGLibType() types.GType {
	return xStreamFsGLibType()
}

func StreamFsNewFromInternalPtr(ptr uintptr) *StreamFs {
	cls := &StreamFs{}
	cls.Ptr = ptr
	return cls
}

var xNewStreamFsWithFd func(int) uintptr

// Creates a new fs stream using the given file descriptor @fd as the
// backing store. When the stream is destroyed, the file descriptor
// will be closed.
func NewStreamFsWithFd(FdVar int) *StreamFs {
	var cls *StreamFs

	cret := xNewStreamFsWithFd(FdVar)

	if cret == 0 {
		return nil
	}
	cls = &StreamFs{}
	cls.Ptr = cret
	return cls
}

var xNewStreamFsWithName func(string, int, uint32, **glib.Error) uintptr

// Creates a new #CamelStreamFs corresponding to the named file, flags,
// and mode.
func NewStreamFsWithName(NameVar string, FlagsVar int, ModeVar uint32) (*StreamFs, error) {
	var cls *StreamFs
	var cerr *glib.Error

	cret := xNewStreamFsWithName(NameVar, FlagsVar, ModeVar, &cerr)

	if cret == 0 {
		return nil, cerr
	}
	cls = &StreamFs{}
	cls.Ptr = cret
	if cerr == nil {
		return cls, nil
	}
	return cls, cerr

}

var xStreamFsGetFd func(uintptr) int

func (x *StreamFs) GetFd() int {

	cret := xStreamFsGetFd(x.GoPointer())
	return cret
}

func (c *StreamFs) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *StreamFs) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// Tests if the stream supports the #GSeekableIface.
func (x *StreamFs) CanSeek() bool {

	cret := gio.XGSeekableCanSeek(x.GoPointer())
	return cret
}

// Tests if the length of the stream can be adjusted with
// g_seekable_truncate().
func (x *StreamFs) CanTruncate() bool {

	cret := gio.XGSeekableCanTruncate(x.GoPointer())
	return cret
}

// Seeks in the stream by the given @offset, modified by @type.
//
// Attempting to seek past the end of the stream will have different
// results depending on if the stream is fixed-sized or resizable.  If
// the stream is resizable then seeking past the end and then writing
// will result in zeros filling the empty space.  Seeking past the end
// of a resizable stream and reading will result in EOF.  Seeking past
// the end of a fixed-sized stream will fail.
//
// Any operation that would result in a negative offset will fail.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned.
func (x *StreamFs) Seek(OffsetVar int64, TypeVar glib.SeekType, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableSeek(x.GoPointer(), OffsetVar, TypeVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Tells the current position within the stream.
func (x *StreamFs) Tell() int64 {

	cret := gio.XGSeekableTell(x.GoPointer())
	return cret
}

// Sets the length of the stream to @offset. If the stream was previously
// larger than @offset, the extra data is discarded. If the stream was
// previously shorter than @offset, it is extended with NUL ('\0') bytes.
//
// If @cancellable is not %NULL, then the operation can be cancelled by
// triggering the cancellable object from another thread. If the operation
// was cancelled, the error %G_IO_ERROR_CANCELLED will be returned. If an
// operation was partially finished when the operation was cancelled the
// partial result will be returned, without an error.
func (x *StreamFs) Truncate(OffsetVar int64, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGSeekableTruncate(x.GoPointer(), OffsetVar, CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

func init() {
	core.SetPackageName("CAMEL", "camel-1.2")
	core.SetSharedLibraries("CAMEL", []string{"libcamel-1.2.so.66"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("CAMEL") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xStreamFsGLibType, libs, "camel_stream_fs_get_type")

	core.PuregoSafeRegister(&xNewStreamFsWithFd, libs, "camel_stream_fs_new_with_fd")
	core.PuregoSafeRegister(&xNewStreamFsWithName, libs, "camel_stream_fs_new_with_name")

	core.PuregoSafeRegister(&xStreamFsGetFd, libs, "camel_stream_fs_get_fd")

}

// Package edataserver was automatically generated by github.com/jwijenbergh/puregotk DO NOT EDIT
package edataserver

import (
	"structs"
	"unsafe"

	"github.com/jwijenbergh/purego"
	"github.com/jwijenbergh/puregotk/pkg/core"
	"github.com/jwijenbergh/puregotk/v4/gio"
	"github.com/jwijenbergh/puregotk/v4/glib"
	"github.com/jwijenbergh/puregotk/v4/gobject"
	"github.com/jwijenbergh/puregotk/v4/gobject/types"
)

type NetworkMonitorClassGType struct {
	_ structs.HostLayout

	ParentClass gobject.ObjectClassGType
}

func (x *NetworkMonitorClassGType) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

type NetworkMonitorPrivate struct {
	_ structs.HostLayout
}

func (x *NetworkMonitorPrivate) GoPointer() uintptr {
	return uintptr(unsafe.Pointer(x))
}

const (
	// A special name, which can be used as a GIO name in the call
	// to e_network_monitor_set_gio_name(), which is used to report
	// the network as always reachable.
	NETWORK_MONITOR_ALWAYS_ONLINE_NAME string = "\"always-online\""
)

// Contains only private data that should be read and manipulated using
// the functions below. Implements #GNetworkMonitorInterface.
type NetworkMonitor struct {
	gobject.Object
}

var xNetworkMonitorGLibType func() types.GType

func NetworkMonitorGLibType() types.GType {
	return xNetworkMonitorGLibType()
}

func NetworkMonitorNewFromInternalPtr(ptr uintptr) *NetworkMonitor {
	cls := &NetworkMonitor{}
	cls.Ptr = ptr
	return cls
}

var xNetworkMonitorDupGioName func(uintptr) string

// Get currently set GIO name for the network availability checks.
// See e_network_monitor_set_gio_name() for more details.
func (x *NetworkMonitor) DupGioName() string {

	cret := xNetworkMonitorDupGioName(x.GoPointer())
	return cret
}

var xNetworkMonitorListGioNames func(uintptr) *glib.SList

// Get a list of available GIO names for the #GNetworkMonitor implementations.
// The strings can be used in e_network_monitor_set_gio_name().
func (x *NetworkMonitor) ListGioNames() *glib.SList {

	cret := xNetworkMonitorListGioNames(x.GoPointer())
	return cret
}

var xNetworkMonitorSetGioName func(uintptr, string)

// Set a @gio_name of the #GNetworkMonitor implementation to use, can be %NULL.
// Use e_network_monitor_list_gio_names() for a list of available
// implementations. A special value, %E_NETWORK_MONITOR_ALWAYS_ONLINE_NAME, can
// be used to report the network as always reachable. When an unknown GIO
// name is used the default #GNetworkMonitor implementation, as returned
// by the g_network_monitor_get_default(), will be used.
func (x *NetworkMonitor) SetGioName(GioNameVar string) {

	xNetworkMonitorSetGioName(x.GoPointer(), GioNameVar)

}

func (c *NetworkMonitor) GoPointer() uintptr {
	if c == nil {
		return 0
	}
	return c.Ptr
}

func (c *NetworkMonitor) SetGoPointer(ptr uintptr) {
	c.Ptr = ptr
}

// SetPropertyGioName sets the "gio-name" property.
// The GIO name of the underlying #GNetworkMonitor to use.
func (x *NetworkMonitor) SetPropertyGioName(value string) {
	var v gobject.Value
	v.Init(gobject.TypeStringVal)
	v.SetString(value)
	x.SetProperty("gio-name", &v)
}

// GetPropertyGioName gets the "gio-name" property.
// The GIO name of the underlying #GNetworkMonitor to use.
func (x *NetworkMonitor) GetPropertyGioName() string {
	var v gobject.Value
	x.GetProperty("gio-name", &v)
	return v.GetString()
}

// Initializes the object implementing the interface.
//
// This method is intended for language bindings. If writing in C,
// g_initable_new() should typically be used instead.
//
// The object must be initialized before any real use after initial
// construction, either with this function or g_async_initable_init_async().
//
// Implementations may also support cancellation. If @cancellable is not %NULL,
// then initialization can be cancelled by triggering the cancellable object
// from another thread. If the operation was cancelled, the error
// %G_IO_ERROR_CANCELLED will be returned. If @cancellable is not %NULL and
// the object doesn't support cancellable initialization the error
// %G_IO_ERROR_NOT_SUPPORTED will be returned.
//
// If the object is not initialized, or initialization returns with an
// error, then all operations on the object except g_object_ref() and
// g_object_unref() are considered to be invalid, and have undefined
// behaviour. See the [description][iface@Gio.Initable#description] for more details.
//
// Callers should not assume that a class which implements #GInitable can be
// initialized multiple times, unless the class explicitly documents itself as
// supporting this. Generally, a class’ implementation of init() can assume
// (and assert) that it will only be called once. Previously, this documentation
// recommended all #GInitable implementations should be idempotent; that
// recommendation was relaxed in GLib 2.54.
//
// If a class explicitly supports being initialized multiple times, it is
// recommended that the method is idempotent: multiple calls with the same
// arguments should return the same results. Only the first call initializes
// the object; further calls return the result of the first call.
//
// One reason why a class might need to support idempotent initialization is if
// it is designed to be used via the singleton pattern, with a
// #GObjectClass.constructor that sometimes returns an existing instance.
// In this pattern, a caller would expect to be able to call g_initable_init()
// on the result of g_object_new(), regardless of whether it is in fact a new
// instance.
func (x *NetworkMonitor) Init(CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGInitableInit(x.GoPointer(), CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Attempts to determine whether or not the host pointed to by
// @connectable can be reached, without actually trying to connect to
// it.
//
// This may return %TRUE even when #GNetworkMonitor:network-available
// is %FALSE, if, for example, @monitor can determine that
// @connectable refers to a host on a local network.
//
// If @monitor believes that an attempt to connect to @connectable
// will succeed, it will return %TRUE. Otherwise, it will return
// %FALSE and set @error to an appropriate error (such as
// %G_IO_ERROR_HOST_UNREACHABLE).
//
// Note that although this does not attempt to connect to
// @connectable, it may still block for a brief period of time (eg,
// trying to do multicast DNS on the local network), so if you do not
// want to block, you should use g_network_monitor_can_reach_async().
func (x *NetworkMonitor) CanReach(ConnectableVar gio.SocketConnectable, CancellableVar *gio.Cancellable) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGNetworkMonitorCanReach(x.GoPointer(), ConnectableVar.GoPointer(), CancellableVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Asynchronously attempts to determine whether or not the host
// pointed to by @connectable can be reached, without actually
// trying to connect to it.
//
// For more details, see g_network_monitor_can_reach().
//
// When the operation is finished, @callback will be called.
// You can then call g_network_monitor_can_reach_finish()
// to get the result of the operation.
func (x *NetworkMonitor) CanReachAsync(ConnectableVar gio.SocketConnectable, CancellableVar *gio.Cancellable, CallbackVar *gio.AsyncReadyCallback, UserDataVar uintptr) {

	gio.XGNetworkMonitorCanReachAsync(x.GoPointer(), ConnectableVar.GoPointer(), CancellableVar.GoPointer(), glib.NewCallbackNullable(CallbackVar), UserDataVar)

}

// Finishes an async network connectivity test.
// See g_network_monitor_can_reach_async().
func (x *NetworkMonitor) CanReachFinish(ResultVar gio.AsyncResult) (bool, error) {
	var cerr *glib.Error

	cret := gio.XGNetworkMonitorCanReachFinish(x.GoPointer(), ResultVar.GoPointer(), &cerr)
	if cerr == nil {
		return cret, nil
	}
	return cret, cerr

}

// Gets a more detailed networking state than
// g_network_monitor_get_network_available().
//
// If #GNetworkMonitor:network-available is %FALSE, then the
// connectivity state will be %G_NETWORK_CONNECTIVITY_LOCAL.
//
// If #GNetworkMonitor:network-available is %TRUE, then the
// connectivity state will be %G_NETWORK_CONNECTIVITY_FULL (if there
// is full Internet connectivity), %G_NETWORK_CONNECTIVITY_LIMITED (if
// the host has a default route, but appears to be unable to actually
// reach the full Internet), or %G_NETWORK_CONNECTIVITY_PORTAL (if the
// host is trapped behind a "captive portal" that requires some sort
// of login or acknowledgement before allowing full Internet access).
//
// Note that in the case of %G_NETWORK_CONNECTIVITY_LIMITED and
// %G_NETWORK_CONNECTIVITY_PORTAL, it is possible that some sites are
// reachable but others are not. In this case, applications can
// attempt to connect to remote servers, but should gracefully fall
// back to their "offline" behavior if the connection attempt fails.
func (x *NetworkMonitor) GetConnectivity() gio.NetworkConnectivity {

	cret := gio.XGNetworkMonitorGetConnectivity(x.GoPointer())
	return cret
}

// Checks if the network is available. "Available" here means that the
// system has a default route available for at least one of IPv4 or
// IPv6. It does not necessarily imply that the public Internet is
// reachable. See #GNetworkMonitor:network-available for more details.
func (x *NetworkMonitor) GetNetworkAvailable() bool {

	cret := gio.XGNetworkMonitorGetNetworkAvailable(x.GoPointer())
	return cret
}

// Checks if the network is metered.
// See #GNetworkMonitor:network-metered for more details.
func (x *NetworkMonitor) GetNetworkMetered() bool {

	cret := gio.XGNetworkMonitorGetNetworkMetered(x.GoPointer())
	return cret
}

var xNetworkMonitorGetDefault func() uintptr

// Gets the default #ENetworkMonitor. The caller should not unref the returned instance.
// The #ENetworkMonitor implements the #GNetworkMonitor iterface.
func NetworkMonitorGetDefault() *gio.NetworkMonitorBase {
	var cls *gio.NetworkMonitorBase

	cret := xNetworkMonitorGetDefault()

	if cret == 0 {
		return nil
	}
	gobject.IncreaseRef(cret)
	cls = &gio.NetworkMonitorBase{}
	cls.Ptr = cret
	return cls
}

func init() {
	core.SetPackageName("EDATASERVER", "libedataserver-1.2")
	core.SetSharedLibraries("EDATASERVER", []string{"libcamel-1.2.so.66", "libedataserver-1.2.so.27"})
	var libs []uintptr
	for _, libPath := range core.GetPaths("EDATASERVER") {
		lib, err := purego.Dlopen(libPath, purego.RTLD_NOW|purego.RTLD_GLOBAL)
		if err != nil {
			panic(err)
		}
		libs = append(libs, lib)
	}

	core.PuregoSafeRegister(&xNetworkMonitorGLibType, libs, "e_network_monitor_get_type")

	core.PuregoSafeRegister(&xNetworkMonitorDupGioName, libs, "e_network_monitor_dup_gio_name")
	core.PuregoSafeRegister(&xNetworkMonitorListGioNames, libs, "e_network_monitor_list_gio_names")
	core.PuregoSafeRegister(&xNetworkMonitorSetGioName, libs, "e_network_monitor_set_gio_name")

	core.PuregoSafeRegister(&xNetworkMonitorGetDefault, libs, "e_network_monitor_get_default")

}
